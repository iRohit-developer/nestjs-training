import { ErrorMappingInterceptor } from './error-mapping.interceptor';

describe('ErrorMappingInterceptor', () => {
  it('should be defined', () => {
    expect(new ErrorMappingInterceptor()).toBeDefined();
  });
});
